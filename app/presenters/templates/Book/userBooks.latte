{block content}
{foreach $books as $book}
 <div><a href="{link Book:show id=>$book->id}"> {$book->name} </a></div>
         {form deleteBookFromUserForm}
          <input type="hidden" name="bookId" value="{$book->id}" >
          <input type = "submit" value = "odebrat">
        {/form}
{/foreach}	
